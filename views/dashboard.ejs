<!DOCTYPE html>
<html lang="en">
<%- include('includes/head') %> <a href="/delete-account">Radera konto!</a>

<body>
    <%- include('includes/header') %>
    <main>
        <% if (posts.length == 0) { %>
            <h4>Hej <%= user.username %>, dina inlägg kommer sparas här nedanför!</h4>
        <% } else { %>
       <h4>Hej <%= user.username %>, här är dina inlägg!</h4>
       <% } %>
       
       <ul>
        <% posts.forEach(post => { %>
            <li><a href="/post/<%= post.id %>"> <%= post.title %> </a><span>-</span> skapat <%= new Date(post.createdDate).getFullYear() %> <%= new Date(post.createdDate).getMonth() +1 %> <%= new Date(post.createdDate).getDate() %></li>

        <% })  %>
       </ul>

       <h4>Här är övriga inlägg</h4>
       <ul>
        <% otherPosts.forEach(post => { %>
            <li><a href="/post/<%= post.id %>"> <%= post.title %> </a><span>-</span> skapat <%= new Date(post.createdDate).getFullYear() %> <%= new Date(post.createdDate).getMonth() +1 %> <%= new Date(post.createdDate).getDate() %>  postat av  <%= post.username %></li>

        <% })  %>
       </ul>
       
    </main>    
    <%- include('includes/footer') %>
    
</body>
</html>